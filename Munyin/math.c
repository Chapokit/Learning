#include <stdio.h>
#include <math.h>


int main(){

    double A = sqrt(9);
    double B = pow(2,4);
    int C = round(3.14);
    int D = ceil(3.14);
    int E = floor(3.99);
    double F = fabs(-100); //absolite
    double G = log(3);
    // sin cos tan
    // %lf long float

    printf("\n%lf", F);
}